# Resume matrix before spawn

Source is implemented and locally tested; independent implementation review, supervisor sign-off and live rollout remain pending. Start at `docs/divybot-matrix.md`, then `cmd/divybot/matrix.go` and `matrix_test.go`. The process bridge consumes the pinned NetScript authority in place. All observations remain unknown, and evaluator admission refuses.

The approved plan and owner dispositions are in Harness PR #339. Follow-up review must not mistake tests or a pane handle for actual independent model/session observation. Do not deploy or mark this draft ready merely because CI is green. Preserve private configuration and receipt storage outside Git.

Owner clarification recorded: this lane owns the Orchid spawn hook; upstream PR2 remains draft
and Orchid merge is unauthorized. Fork 3 refuses evaluator launches with an explicit inconclusive
`observer-unavailable` diagnostic from the existing receipt vocabulary. No native observer is
added. Fork 4 retains configurable pins and the trusted owner override; the override must also
name the configured pin it authorizes. The conversation reply supplies authority for all three
questions. There is no separate unanswered question set and no mailbox wait. Harness PRs remain
drafts until the owner signs off.
