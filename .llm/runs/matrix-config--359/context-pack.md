Matrix configuration build and preflight

Owner requested a placeholder-only example, a deterministic builder for pinned source and complete-brief grants, and a validator before dispatch. This slice reuses MatrixConfig, MatrixGrant, prepareMatrixRequest, briefDigest and the existing source bridge. It depends on the refusal diagnostic slice for safe errors. No live config, dispatcher state, source revision or operator identity is published.

Mutation scope is the two CLI subcommands, example matrix block, tests and operator documentation. Build preserves prior authority and unrelated config, writes a new private candidate, and fails rather than advancing an existing source pin. Validation invokes no coordinator, host command, launch or GitHub mutation. Live quota/placement are explicitly unchecked. Ordinary matrix routes do not require unconditional grants; privileged and deviating routes retain source authority requirements.

NetScript MCP search_docs found no public delegation-matrix match; the code in matrix.go and its existing bridge remains the runtime contract. No copied model catalog or default pin set is introduced. Independent supervisor sign-off and the live alpha dispatch are not claimed.
